# MenuPocket 验证记录

## 1. 当前范围

0.3.0-alpha.1 只提供分组管理、排序、组入口和原菜单点击。原图标隐藏与移动功能已移除，旧版隐藏测试不作为本版本的能力证明。

| 层次 | 覆盖内容 | 验证边界 |
| --- | --- | --- |
| 模型测试 | 分组、删除组保留图标、标识迁移、v1 配置备份与 v2 往返、损坏文件保护 | 不证明第三方菜单点击成功 |
| 构建检查 | Swift 编译、签名、双架构、DMG 挂载与校验 | 不替代真实桌面交互 |
| 文档截图 | 实际 SwiftUI 视图与隔离演示数据 | 不包含真实用户应用列表 |
| 真实桌面 | 旧版在 macOS 26.6.2 / Apple Silicon 有开发测试记录 | 新版原菜单点击尚未逐应用验收 |

## 2. 复验步骤

1. 退出旧版再启动本版，确认没有旧进程留下的占位。
2. 检查辅助功能授权与扫描结果；仅看到系统开关开启不足以证明授权有效。
3. 新建组，将图标移入、移出和排序，确认原应用图标没有被移动或隐藏。
4. 点击管理卡片应只显示分组和排序设置；菜单栏组入口应打开小面板。
5. 从面板打开原菜单，测试快捷键和关闭面板；逐项记录目标应用结果。
6. 使用 v1 配置升级，核对备份文件、组名、组入口状态、顺序和名称；重启后仍保留。

## 3. 已知限制

识别与点击依赖原应用辅助功能接口。原菜单仍在原应用的位置打开，屏幕外或刘海下的图标若没有有效 AX 动作，可能无法点击。图标分组不会为系统菜单栏释放空间。

安装版与开发版签名可能不同，升级后可能需要重新授权。当前仍为未经过 Apple 公证的 Alpha 预览版。CI 和截图导出不会自动获得辅助功能权限。
